# Homebrew Tap for cloud-deploy

This tap provides the `cloud-deploy` tool for macOS and Linux.

## Installation

```bash
brew tap jvreagan/tap
brew install cloud-deploy
```

## Available Formulae

- **cloud-deploy** - Manifest-driven multi-cloud deployment tool
